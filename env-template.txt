# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=yatra_db
DB_PORT=3306

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret - REQUIRED: Set a strong, random secret key
JWT_SECRET=

# CORS Configuration
CORS_ORIGIN=http://localhost:5500,http://127.0.0.1:5500

